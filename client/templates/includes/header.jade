template(name='header')
   nav.navbar.navbar-default(role='navigation')
      .container-fluid
         .navbar-header
            button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navigation')
               span.sr-only Toggle navigation
               span.icon-bar
               span.icon-bar
               span.icon-bar
            a.navbar-brand(href='{{pathFor "postsList"}}') Microscope
         #navigation.collapse.navbar-collapse
            ul.nav.navbar-nav
               li
                  a(href='{{pathFor "newPosts"}}') New
               li
                  a(href='{{pathFor "bestAdded routes for post lists, and pages to display them.Posts"}}') Best
               +if currentUser
                  li
                     a(href='{{pathFor "postSubmit"}}') Submit Post
                  li.dropdown
                     +notifications
            ul.nav.navbar-nav.navbar-right
               +loginButtons
